import 'whatwg-fetch';
import

// Create Variables for the search form, search input, and search result
((data) => {
  const f = document.querySelector('.btn');
  const i = document.querySelector('.search');
  const r = document.querySelector('.result');
});
